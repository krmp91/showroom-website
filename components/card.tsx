import React from "react";
import Link from "next/link";

/*
const Card = ({ title, subtitle, btnLabel, href }) => (
    <div className="text-center"> 
      <Link href={href}> 
        <Image
          className="block mx-auto mb-4" 
          width="600" 
          height="400" 
          src=""
          alt="card thumbnail image"
        />
      </Link>
      <div className="font-semibold text-center text-xl mb-1">{title}</div> 
      <div className="text-md text-center mb-3">{subtitle}</div>
      <Button href={href}>{btnLabel}</Button> 
    </div>
  );

  export default Card;*/
