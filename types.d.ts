/*type CategoryNode = {
  name: string;
};

type PostNode = {
  title: string;
  slug: string;
  id: string;
  date: string;
  content: string;
  uri: string;
  categories: {
    nodes: CategoryNode[];
  };
  featuredImage: {
    node: {
      sourceUrl: string;
    };
  };
};

type Posts = {
  title: ReactNode;
  data: {
    posts: {
      nodes: PostNode[];
    };
  };
};*/
